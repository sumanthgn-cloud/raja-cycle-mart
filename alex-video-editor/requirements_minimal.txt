# Minimal requirements (without Whisper for now)
# Install with: pip install -r requirements_minimal.txt

# Core video processing
moviepy==2.1.2
pydub==0.25.1

# Audio processing
librosa==0.10.1
soundfile==0.12.1

# Utilities
numpy
pillow>=9.2.0,<11.0
tqdm
pyyaml==6.0.1

# Note: Whisper requires PyTorch which has Windows path length issues
# We'll add captions manually or use a simpler solution
